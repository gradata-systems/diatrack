apiVersion: v1
kind: Service
metadata:
  name: {{ include "diatrack-api.fullname" . }}
  labels:
    {{- include "diatrack-api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diatrack-api.selectorLabels" . | nindent 4 }}
