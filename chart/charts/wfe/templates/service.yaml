apiVersion: v1
kind: Service
metadata:
  name: {{ include "diatrack-wfe.fullname" . }}
  labels:
    {{- include "diatrack-wfe.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "diatrack-wfe.selectorLabels" . | nindent 4 }}
